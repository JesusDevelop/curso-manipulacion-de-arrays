 let letter = ['a','b','c','d'];
//forEach
letter.forEach(element => console.log(element));
letter.map(element=>console.log(element));
 //for normal
 for(let i=0; i<letter.length ; i++){

    console.log(letter[i]);

 }